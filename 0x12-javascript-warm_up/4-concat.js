#!/usr/bin/node

/**
 * A script that prints two arguments passed to it, in the following format:
 * “ is ”
 * Example - "arg_2" "is" "arg_3"
 */
console.log(process.argv[2] + ' is ' + process.argv[3]);
